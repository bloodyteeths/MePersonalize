{% schema %}
{
  "name": "Personalizer Widget",
  "target": "section",
  "stylesheet": "personalizer.css",
  "javascript": "personalizer.js",
  "settings": [
    {
      "type": "range",
      "id": "char_limit",
      "min": 10,
      "max": 500,
      "step": 10,
      "label": "Character limit",
      "default": 100
    },
    {
      "type": "checkbox",
      "id": "required",
      "label": "Require personalization",
      "default": false
    }
  ]
}
{% endschema %}

{% assign personalization_label = product.metafields.custom.personalization_label.value %}

{% if personalization_label != blank %}
  <div class="personalizer-container" data-product-id="{{ product.id }}">
    <div class="personalizer-field">
      <label class="personalizer-label" for="personalizer-input">
        {{ personalization_label }}
      </label>
      <textarea
        id="personalizer-input"
        class="personalizer-input"
        placeholder="Enter your personalization here..."
        maxlength="{{ block.settings.char_limit }}"
        {% if block.settings.required %}data-required="true"{% endif %}
      ></textarea>
      <div class="personalizer-char-count">
        <span id="personalizer-count">0</span> / {{ block.settings.char_limit }}
      </div>
      <div class="personalizer-error" id="personalizer-error" style="display: none;">
        This field is required.
      </div>
    </div>

    <!-- Hidden input for form submission -->
    <input type="hidden" name="properties[Personalization]" id="prop-personalization">
  </div>
{% endif %}
